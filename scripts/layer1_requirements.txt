# Layer 1 character tracking — local test deps (scripts/ is gitignored)

# Video branch: YOLOv11 + ByteTrack + ArcFace
ultralytics>=8.3
supervision>=0.24
insightface>=0.7
onnxruntime>=1.17
opencv-python>=4.8
numpy>=1.24
scipy>=1.10

# Optional future phases (not required for core Layer 1 video tracking):
# openai-whisper
# pyannote.audio
# transformers  # Qwen2.5-VL
